{#
    card (contentype recrutement):
        title: text
        teaser: text
        profile: text
        description:
            salary: number
            temps: select (text)
#}

{% block stylesheet %}
    <link rel="stylesheet" href="{{ asset('css/cardRecrutement.css') }}">
{% endblock %}

<div class="card-recrutement col-12 col-xl-3">
    <div class="block-image">
        <img class="w-100 h-100" src="{{ asset('images/banner-recrutement-card.png') }}" alt="bannière recrutement">
    </div>
    <div class="block-content">
        <h3 class="title">{{ card.title }}</h3>
        <p class="teaser">{{ card.teaser }}</p>
        <p class="description">{{ card.description }}</p>
        {% include "@theme/partials/buttons/_button-red.twig" with { text: "Candidater rapidement", link: '#', width: "262px", height: "28px" } %}
    </div>
</div>
