{% macro display_menu_item(item, loop, withsubmenus) %}
    {% apply spaceless %}
<li class="footer-list-item">
   <img src="{{ asset('images/arrowRight.svg') }}"> <a href="/{{ item.link }}" title="{{ item.title }}" aria-current="page" class="">{{- item.label -}}</a>
</li>
    {% endapply %}
{% endmacro %}

{# Make the macro available for use #}
{% from _self import display_menu_item %}
<ul class="footer-list">
    {% for item in menu %}
        {% if item.label is defined %}
            {{ display_menu_item(item, loop, withsubmenus) }}
        {% endif %}
    {% endfor %}
</ul>
