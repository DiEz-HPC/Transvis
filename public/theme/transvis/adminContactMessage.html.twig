{% extends '@bolt/_base/layout.html.twig' %}

{% block title %}
    Listes des messages
{% endblock %}
{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{asset('css/adminContact.css')}}">
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script src="{{ asset('js/_adminContact/adminContactButton.js') }}"></script>
    <script src="{{ asset('js/_adminContact/pagination.js') }}"></script>
{% endblock %}
{% block main %}
    <div class="container">
        {% include 'partials/_appFlash.twig' %}
        <div class="row">
            <div class="col-12" id="contactMessage" data-pagination="1" data-nbpages="{{ nbPages }}">
                {% include 'partials/_adminContactMessage.twig' %}
            </div>
            <button class="btn btn-primary" id="previousBtn">Page précedente</button>
            <button class="btn btn-primary" id="nextBtn">Page suivante</button>
        </div>
        {% include 'partials/_adminContact/_replyModal.twig' %}
        <div class="d-flex justify-content-center w-100">
        {# {{ pager(contactMessages, template = '@theme/helpers/_pager.html.twig') }}  #}
        </div>
    </div>
{% endblock %}
